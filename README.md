## Thepla brand marketing + catalog website with brand story, product category sliders, cart, and contact/FAQ pages

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
